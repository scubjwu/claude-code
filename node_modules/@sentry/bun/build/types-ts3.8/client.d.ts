import { ServerRuntimeClient } from '@sentry/core';
import { BunClientOptions } from './types';
/**
 * @deprecated This client is no longer used in v9.
 */
export declare class BunClient extends ServerRuntimeClient<BunClientOptions> {
    /**
     * Creates a new Bun SDK instance.
     * @param options Configuration options for this SDK.
     */
    constructor(options: BunClientOptions);
}
//# sourceMappingURL=client.d.ts.map
